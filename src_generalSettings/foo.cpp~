#include <queue>
#include <iostream>
#include <algorithm>
#include <set>
#include <map>
#include <vector>
#include <sstream>      /* sstream */
#include <fstream>
#include <string>
#include <stdlib.h>     /* exit, EXIT_FAILURE */
#include <algorithm>    // std::sort
//#include "fooA.h"
#include <ctype.h>

//#include "maths.h"
//#include "generateFiles.h"

using namespace std;

const int essai(5);

int main(int argc, char* const argv[])
{
    map<int, vector<int>> genes_map;
    
    if(genes_map[2].empty()){
    cout << "hello" <<endl;
    genes_map[2].push_back(5);
}
        if(genes_map[2].empty()){
    cout << "hello2" <<endl;

}

    genes_map[1].push_back(1);
    
    genes_map[3].push_back(4);
    
    vector<int> pos;
    
    pos.push_back(5);pos.push_back(10);pos.push_back(2);pos.push_back(3);pos.push_back(6);
    sort(pos.begin(),pos.begin()+3 );
    for(auto x : pos){
    cout << x << endl;
    }
    
    int myVar(5);

    if(find(begin(genes_map[3]), end(genes_map[3]),myVar) != end(genes_map[3])) cout  << myVar << " was  in pos" << endl;
   
   cout << genes_map.size() << " SIZE" << endl;
   
   for(int i = 0; !genes_map.empty(); i++)
   {
    genes_map.erase(i);
    cout << "*" << i << endl;
    
   }
   
   string s = "a";
   char a = s[0];
   char b = toupper(a);
   cout << b <<endl;
   
   
   
    return 0;
}

